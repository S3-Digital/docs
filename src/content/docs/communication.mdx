---
title: Panel Komunikacji Operacyjnej
---

import timeline from '../../data/timeline.json';
import { Badge, Steps } from '@astrojs/starlight/components';

## 🕒 Ostatnie ustalenia zespołowe

<Steps>
{timeline.map((item) => (
  <li>
    <strong>{item.date}</strong> — {item.team} 
    <Badge text={item.status === 'done' ? 'ZAKOŃCZONE' : 'W TOKU'} variant={item.status === 'done' ? 'success' : 'note'} />
    <br/>
    _{item.task}_ (Zgłosił: {item.author})
  </li>
))}
</Steps>

> **Testowanie:** Dodaj nowy obiekt do pliku `src/data/timeline.json` na GitHubie i sprawdź, czy pojawił się na górze tej listy.