---
interface Props {
    data?: {
        ssl_expiry?: string;
        status?: string;
        provider?: string;
    };
}

// Pobieramy dane z Props, ale tworzymy "bezpiecznik" (pusty obiekt jako fallback)
const { data = {} } = Astro.props;

// Używamy opcjonalnego łańcuchowania, żeby uniknąć błędu 'undefined'
const expiryDate = data?.ssl_expiry || "Brak danych";
const status = data?.status || "Nieznany";
---

<div class="s-security-card">
    <h4>🔒 Status Certyfikatu SSL</h4>
    <p>Status: <strong>{status}</strong></p>
    <p>Wygasa: <span>{expiryDate}</span></p>
</div>

<style>
    .s-security-card {
        padding: 1rem;
        border: 1px solid #e5e7eb;
        border-radius: 8px;
        background: #fdfdfd;
    }
    strong {
        color: #059669;
    }
</style>
